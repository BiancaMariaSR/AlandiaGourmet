<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ifma.biancamaria.alandiagourmet">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".PizzaSplash"
            android:theme="@style/AppCompat.TelaCheia">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".PedidoActivity"></activity>
        <activity android:name=".RecuperaPedidos"></activity>
        <activity android:name=".ListagemClientesRecyclerview"></activity>
        <activity android:name=".CadastroCliente"></activity>
        <activity android:name=".AlteraCliente"></activity>
    </application>

</manifest>